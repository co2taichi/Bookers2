<%= render 'layouts/errorMessage', error_value: @book%>

<div class = 'container'>
  <div class="row">
    <%= render 'layouts/createform', who_user: current_user %>
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover">
        <thead>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th></th>
          <th></th>
        </thead>
        <% @books.each do |books| %>
          <tr>
            <td width = '10%'>
              <%= link_to user_path(books.user) do %>
                <%= render 'layouts/userimage', who_user: books.user, image_size: "50x50" %>
              <% end %>
            </td>
            <td width = '20%'><span><%= link_to "#{books.title}", book_path(books.id) %></span></td>
            <td width = '27%'><span><%= books.body %></span></td>
            <td width = '8%'>
              <%= render 'layouts/createfavorited', book: books %>
            </td>
            <td width = '25%'>
              <div>
                <p>コメント件数：<%= books.book_comments.count %></p>
              </div>
            </td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>

