<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2Ver2</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/ac02843594.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <header style="height: 56px;">
      <nav class='d-flex navbar navbar-expand-lg navbar-light bg-dark list-unstyled'>
        <%= link_to "Bookers", root_path, class: "text-light navbar-brand" %>
        <ul class='navbar-nav ml-auto'>

          <li>
            <% if user_signed_in? %>
              <%= link_to user_path(current_user.id) , class: "text-light p-xl-2" do %>
                <i class="fas fa-home"></i> Home
              <% end %>
            <% else %>
              <%= link_to root_path, class: "text-light p-xl-2" do %>
                <i class="fas fa-home"></i> Home
              <% end %>
            <% end %>
          </li>
          <% if user_signed_in? %>
            <li>
              <%= link_to users_path, class: "text-light p-xl-2" do %>
                <i class="fas fa-users"></i> Users
              <% end %>
            </li>
            <li>
              <%= link_to books_path, class: "text-light p-xl-2" do %>
                <i class="fas fa-book-open"></i> Books
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete, class: "text-light p-xl-2" do %>
                <i class="fas fa-sign-out-alt"></i> Log out
              <% end %>
            </li>
          <% else %>
            <li>
              <%= link_to about_path, class: "text-light p-xl-2" do %>
                <i class="fas fa-link"></i> About
              <% end %>
            </li>
            <li>
              <%= link_to new_user_registration_path, class: "text-light p-xl-2" do %>
                <i class="fas fa-user-plus"></i> Sign up
              <% end %>
            </li>
            <li>
              <%= link_to new_user_session_path, class: "text-light p-xl-2" do %>
                <i class="fas fa-sign-in-alt"></i> Log in
              <% end %>
            </li>
          <% end %>
        </ul>
      </nav>
    </header>
    <main class="mt-3">
      <div class="container">
        <div class"row d-flex flex-row">
          <%= yield %>
        </div>
      </div>
    </main>
  </body>
  <div class="container mt-5">
    <div class="row">
      <footer class='mx-auto'>
        <p class='mb-3'>CopyRight Infratop.inc</p>
      </footer>
    </div>
  </div>
</html>
